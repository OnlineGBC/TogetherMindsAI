Flask==3.0.3
Flask-SocketIO==5.3.6
Flask-SQLAlchemy==3.1.1
simple-websocket==1.1.0
eventlet==0.36.1
python-dotenv>=1.0.0
cryptography>=42.0.0
pytest>=8.0.0
pytest-watch>=4.2.0
watchdog>=4.0.0
python-docx>=1.1.0
fpdf2>=2.7.0
anthropic>=0.40.0
transformers>=4.40.0,<6.0.0
# torch CPU-only — installed separately in Dockerfile with the CPU-only index URL
# to avoid pulling the 2.5 GB CUDA variant from PyPI.
# Local install: pip install torch --index-url https://download.pytorch.org/whl/cpu
# PostgreSQL driver — used on Cloud Run; harmless to install locally
psycopg2-binary>=2.9.0
# Production WSGI server
gunicorn>=21.0.0
# Database migrations
alembic>=1.13.0
# Browser-based end-to-end tests
pytest-playwright>=0.4.0
