Flask==3.1.3
Flask-SocketIO==5.6.1
Flask-SQLAlchemy==3.1.1
simple-websocket==1.1.0
# eventlet is not used locally (werkzeug threading mode); required on Cloud Run
# (Dockerfile uses --worker-class eventlet)
# gunicorn 25.x requires eventlet>=0.40.3
eventlet==0.41.0
typing-extensions>=4.15.0
grpcio==1.80.0
# Google Secrets Manager — uncomment when wiring up secret retrieval on Cloud Run
# google-cloud-secret-manager>=2.16.0
python-dotenv>=1.0.0
cryptography>=42.0.0
sqlalchemy-utils>=0.41.0
pytest>=8.0.0
pytest-watch>=4.2.0
watchdog>=4.0.0
python-docx>=1.1.0
fpdf2>=2.7.0
anthropic>=0.40.0
transformers>=4.40.0,<6.0.0
# torch CPU-only — installed separately in Dockerfile with the CPU-only index URL
# to avoid pulling the 2.5 GB CUDA variant from PyPI.
# Local install: pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cpu
# PostgreSQL driver — used on Cloud Run; harmless to install locally
psycopg2-binary>=2.9.0
# Production WSGI server
gunicorn==25.3.0
# Database migrations
alembic>=1.13.0
# Browser-based end-to-end tests
pytest-playwright>=0.4.0
