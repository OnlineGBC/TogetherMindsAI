Flask==3.1.3
Flask-Limiter==4.1.1
Flask-SocketIO==5.6.1
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.46
simple-websocket==1.1.0
# eventlet is not used locally (werkzeug threading mode); required on Cloud Run
# (Dockerfile uses --worker-class eventlet)
# gunicorn 25.x requires eventlet>=0.40.3
eventlet==0.41.0
typing-extensions==4.12.2
grpcio==1.80.0
# Google Secrets Manager — uncomment when wiring up secret retrieval on Cloud Run
# google-cloud-secret-manager>=2.16.0
python-dotenv==1.0.1
# Timezone database for zoneinfo (recording emails stamp times in US Eastern).
# python:3.10-slim may carry no system tzdata, and without it ZoneInfo raises,
# the email fails to build, and the send is silently skipped.
tzdata==2026.2
cryptography==44.0.0
# pinned to 0.41.2 — 0.42.x changed EncryptedType impl from Text to LargeBinary,
# breaking FernetEngine decryption on PostgreSQL (TypeError: string argument without an encoding)
sqlalchemy-utils==0.41.2
apscheduler==3.11.2
pytest==9.0.2
pytest-watch==4.2.0
watchdog==6.0.0
python-docx==1.2.0
fpdf2==2.8.3
anthropic==0.77.0
# OAuth (OpenID Connect) login for clinicians — Google & Microsoft
Authlib==1.3.2
requests==2.32.3
# LiveKit server SDK — mints audio-room join tokens (Phase 1 conferencing)
livekit-api==1.1.0
# PostgreSQL driver — used on Cloud Run; harmless to install locally
psycopg2-binary==2.9.11
# Production WSGI server
gunicorn==25.3.0
# Database migrations
alembic==1.18.4
# Browser-based end-to-end tests
pytest-playwright==0.7.2
# Local semantic retrieval for ICD grounding — fastembed runs BAAI/bge-small-en-v1.5
# via ONNX in-process (no PHI leaves the instance, no API key). Pulls in
# onnxruntime + numpy. The model is pre-baked into the Docker image (see Dockerfile).
fastembed==0.8.0
# Recordings-bucket access (Phase 4 Step 3) — stream the MP4 for the therapist's
# download and delete it at the 30-day retention deadline, via ADC on Cloud Run.
google-cloud-storage>=2.16.0
# Subscription billing (Phase 4 Step 4) — Stripe Checkout + billing portal +
# signed webhooks. No card data touches the app.
stripe>=9.0.0
# Patient-consent demo video (video_demo/) — spoken narration + slide images
edge-tts==7.2.8
pillow==12.2.0
# NDA generator (nda/) — password-encrypts the Word (.docx) file
msoffcrypto-tool==6.0.0
